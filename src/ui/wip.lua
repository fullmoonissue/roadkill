-- These wip variables are the memory of the set values across windows

return {
    -- All items for the future configuration
    wipConfiguration = {},
    -- Configuration file name
    wipFileName = nil,
    -- Type of item
    wipType = nil,
    -- Root key
    wipWhen = nil,
}